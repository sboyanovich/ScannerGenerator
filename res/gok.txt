main(a, b, c) {
    d = 0;

    for(i = 0; i < a; i++) {
        if(b < c) {
            d = d + b;
        } else {
            d = d + c;
        }
        t = b;
        b = b - c;
        c = c - t;
        for(j = 0; j < 19; j++) {
        }
    }

    combination = a + b * (c + d + e + f) - b - d * a;

    return d;
}